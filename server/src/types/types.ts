export type ClerkData = {
    id: string,
    first_name?: string,
    last_name?: string,
    email?: string,
    image_url?: string,
    eventType: string,
}

export type ReviewData = {
    name: string,
    profession: string,
    place: string,
    review: string,
}